#include <stdio.h>

int main(){
    char ch='e';
    printf("%c\n",ch);
    printf("%d\n",ch);
    if(ch>=97 && ch<=122){
        printf("It is an lower case letter!");
    } else {
        printf("It is not an lowercase letter!");
    }

    return 0;
}
